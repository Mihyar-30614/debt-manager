# Server Configuration
PORT=8100

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-password
DB_NAME=debtapp

# TLS/HTTPS Configuration (optional)
# TLS_CERT_FILE=/path/to/cert.pem
# TLS_KEY_FILE=/path/to/key.pem

# Session and CSRF Keys (auto-generated if not set)
# SESSION_KEY=your-session-key-here
# CSRF_KEY=your-csrf-key-here

# SMTP Configuration for Password Reset Emails (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-app-password
# FROM_EMAIL=your-email@example.com
# FROM_EMAIL_NAME=Debt Manager
# (SMTP_PASSWORD and SMTP_FROM are supported as fallbacks)
